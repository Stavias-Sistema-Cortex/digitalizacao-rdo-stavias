name: Córtex hosted keep-warm deploy · retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: Hosted keep-warm deployment is inactive
    runs-on: ubuntu-latest
    steps:
      - name: Record the local-production boundary
        run: |
          echo "The canonical Córtex runtime is the Stavias local server."
          echo "Hosted rollback resources remain preserved, but no worker is deployed."
