name: Córtex hosted keep-warm · retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: Hosted keep-warm is inactive
    runs-on: ubuntu-latest
    steps:
      - name: Record the local-production boundary
        run: |
          echo "The canonical Córtex runtime is the Stavias local server."
          echo "Hosted rollback resources remain preserved, but no keep-warm request is sent."
